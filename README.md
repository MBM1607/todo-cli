# Introduction

This is a Command Line To-Do List program.
It supports adding new tasks and removing tasks.

It stores the tasks in a sqlite database and uses SQLAlchemy ORM to interact with
it.

## Requirements
All the requirements are listen in requirements.txt file.

```pip install -r requirements.txt```
